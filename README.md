# Đấu Trường Học Tập (Learning Arena)

Dự án giáo dục "gamification" (trò chơi hóa) giúp học sinh ôn luyện kiến thức qua các trận đấu trắc nghiệm kịch tính, tích lũy điểm kinh nghiệm (XP) và leo bảng xếp hạng.

## 🚀 Tính năng chính

*   **Gamification**: Hệ thống Level, XP, Avatar theo cấp độ.
*   **Đa dạng môn học**: Toán, Lý, Hóa, Sinh, Anh, Sử, Địa...
*   **Chế độ chơi**: Cá nhân, Đấu với AI (Matrix Bot), Đội nhóm.
*   **Cloud Database**: Lưu trữ dữ liệu thời gian thực với Supabase.
*   **Admin Dashboard**: Quản lý câu hỏi, đồng bộ dữ liệu từ Google Sheets.
*   **Hiệu ứng**: Giao diện Neon, âm thanh sống động, confetti khi chiến thắng.

## 🛠 Công nghệ sử dụng

*   **Frontend**: React, TypeScript, Vite.
*   **Styling**: Tailwind CSS, Lucide Icons.
*   **Animation**: Framer Motion, Canvas Confetti.
*   **Database**: Supabase (PostgreSQL).
*   **Math Rendering**: KaTeX.

## ⚙️ Cài đặt & Chạy dự án

1.  Clone dự án về máy.
2.  Cài đặt thư viện:
    ```bash
    npm install
    ```
3.  Chạy server development:
    ```bash
    npm start
    ```

## 🗄 Cấu hình Database (Supabase)

Để dự án hoạt động, bạn cần tạo Project trên [Supabase](https://supabase.com/) và chạy đoạn SQL sau trong **SQL Editor** để tạo bảng:

```sql
-- 1. Bảng câu hỏi
create table questions (
  id text primary key,
  subject text,
  type text,
  difficulty text,
  level int,
  text text,
  options text[],
  answer text,
  matching_pairs jsonb
);

-- 2. Bảng tài khoản
create table accounts (
  id text primary key,
  username text unique,
  password text,
  role text,
  created_at text,
  xp int default 0,
  total_score int default 0,
  unlocked_levels jsonb
);

-- 3. Bảng kết quả thi đấu
create table quiz_results (
  id bigint generated by default as identity primary key,
  username text,
  group_name text,
  group_avatar text,
  subject text,
  difficulty text,
  level int,
  score int,
  correct_count int,
  timestamp text
);
```

## 📝 Tác giả

**Thầy Đạt** - Phát triển hệ thống giáo dục 4.0.
